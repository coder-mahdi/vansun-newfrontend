export function StudioStorySection({ className }: { className?: string }) {
  return (
    <section className={className} aria-labelledby="studio-story-heading">
      <h2 id="studio-story-heading">Our story</h2>
    </section>
  );
}
