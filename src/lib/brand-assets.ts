/**
 * Paths under `public/brand/` (favicon pack + logo subfolders).
 * @see public/brand/PLACE_BRAND_ASSETS.txt
 */
const brandBase = "/brand";

export const brandLogoPath = `${brandBase}/logo/logo.png`;

/** Default Open Graph / social share image (`public/` path). */
export const brandDefaultShareImagePath = "/images/Shop/salon.webp";

export const brandFaviconDir = `${brandBase}/favicon`;

export const brandFaviconIcoPath = `${brandFaviconDir}/favicon.ico`;

export const brandFaviconSvgPath = `${brandFaviconDir}/favicon.svg`;

export const brandAppleTouchIconPath = `${brandFaviconDir}/apple-touch-icon.png`;

export const brandWebManifestPath = `${brandFaviconDir}/site.webmanifest`;
